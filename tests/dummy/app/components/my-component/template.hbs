{{#ember-filter onChange=(action 'onChange') as |filters|}}
    {{filters.add availableFilters=model}}
    {{filters.list}}
{{/ember-filter}}


Received filters:
<br>
{{#each filters as |filter|}}
  Name:{{filter.name}}
  <br>
  Value: {{filter.value}}
{{/each}}
